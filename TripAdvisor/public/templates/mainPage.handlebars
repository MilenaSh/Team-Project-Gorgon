<div class="row">
{{#each objects}}
<div class="col-sm-6 col-md-4">
    <div class="thumbnail">
        <div class="caption">
            {{!--{{{img image}}}--}}
            <h3>{{name}}</h3>
            <p> {{description}}</p>
            {{{buttonsLinks id}}}
        </div>
    </div>
</div>
{{/each}}
</div>

<div class="row text-center">
    <ul class="pagination">
    {{#each pages}}
        <li>{{{objectPage this}}}</li>
    {{/each}}
    </ul>
</div>