{{#each hotels}}
<div class="col-sm-6 col-md-4">
    <div class="thumbnail">
        {{!--<img src="{{image}}" alt="...">--}}
        <div class="caption">
            <h3>{{name}}</h3>
            <p> {{description}}</p>
            <p><a href="#" class="btn btn-primary" role="button">View details</a> <a href="#" class="btn btn-default" role="button">Make a Reservation</a></p>
        </div>
    </div>
</div>
{{/each}}